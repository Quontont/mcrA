# unzipping multiple files
ls *tar.gz | xargs -I {}  tar xzvf {} -C .

# Truncating sequences 
seqkit seq -M 740 -m 400 mg_aa.faa -o mg_aa_400_740.faa

# Rename command
mv mg_aa mg_aa.faa

#combining all faa metagenomes to one
cat 2k*/fasta.faa > mg_aa

# cloning/downloading from githun repository
git clone https://github.com/Quontont/mcrA

cat 2s.csv | cut -d " " -f3

#for seperating files into multiple files with lesss objects
cat 2s.csv | parallel --pipe -N 100 "cat >2s_100/2s_{#}.csv"

